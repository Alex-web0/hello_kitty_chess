{
  "rules": {
    ".read": false,
    ".write": false,
    "artifacts": {
      "$appId": {
        "public": {
          "data": {
            "games": {
              "$gameId": {
                ".read": "auth !== null && data.exists() && $gameId.matches(/^COZY-[A-HJ-NP-Z2-9]{8}$/) && data.child('expiresAt').isNumber() && data.child('expiresAt').val() > now && (data.child('whitePlayer').val() === auth.uid || data.child('blackPlayer').val() === auth.uid || data.child('blackPlayer').val() === '')",
                ".write": "auth !== null && newData.exists() && $gameId.matches(/^COZY-[A-HJ-NP-Z2-9]{8}$/) && newData.child('lastActorUid').isString() && newData.child('lastActorUid').val() === auth.uid && newData.child('expiresAt').isNumber() && newData.child('expiresAt').val() > now && newData.child('expiresAt').val() <= now + 7200000 && (!data.exists() || data.child('whitePlayer').val() === auth.uid || data.child('blackPlayer').val() === auth.uid || (data.child('blackPlayer').val() === '' && data.child('whitePlayer').val() !== auth.uid && newData.child('blackPlayer').val() === auth.uid && newData.child('lastAction').val() === 'join'))",
                "gameId": {
                  ".validate": "newData.isString() && newData.val() === $gameId"
                },
                "fen": {
                  ".validate": "newData.isString() && newData.val().length >= 15 && newData.val().length <= 100 && ((!data.exists() && newData.val() === 'rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1') || (data.exists() && (newData.val() === data.val() || (newData.parent().child('lastAction').val() === 'move' && ((data.parent().child('turn').val() === 'w' && data.parent().child('whitePlayer').val() === auth.uid) || (data.parent().child('turn').val() === 'b' && data.parent().child('blackPlayer').val() === auth.uid))) || (newData.parent().child('lastAction').val() === 'reset' && (data.parent().child('whitePlayer').val() === auth.uid || data.parent().child('blackPlayer').val() === auth.uid) && newData.val() === 'rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1'))))"
                },
                "turn": {
                  ".validate": "newData.isString() && newData.val().matches(/^[wb]$/) && ((!data.exists() && newData.val() === 'w') || (data.exists() && (newData.val() === data.val() || (newData.parent().child('lastAction').val() === 'move' && ((data.parent().child('turn').val() === 'w' && data.parent().child('whitePlayer').val() === auth.uid) || (data.parent().child('turn').val() === 'b' && data.parent().child('blackPlayer').val() === auth.uid))) || (newData.parent().child('lastAction').val() === 'reset' && (data.parent().child('whitePlayer').val() === auth.uid || data.parent().child('blackPlayer').val() === auth.uid) && newData.val() === 'w'))))"
                },
                "whitePlayer": {
                  ".validate": "newData.isString() && ((!data.exists() && newData.val() === auth.uid) || (data.exists() && newData.val() === data.val()))"
                },
                "whitePlayerName": {
                  ".validate": "newData.isString() && newData.val().matches(/^[A-Za-z0-9 _-]{1,11}$/) && ((!data.exists() && newData.parent().child('whitePlayer').val() === auth.uid) || (data.exists() && newData.val() === data.val()))"
                },
                "whitePlayerAvatar": {
                  ".validate": "newData.isString() && newData.val().matches(/^[a-z_]+$/) && ((!data.exists() && newData.parent().child('whitePlayer').val() === auth.uid) || (data.exists() && newData.val() === data.val()))"
                },
                "whitePlayerTheme": {
                  ".validate": "newData.isString() && newData.val().matches(/^(kitty|batman)$/) && ((!data.exists() && newData.parent().child('whitePlayer').val() === auth.uid) || (data.exists() && newData.val() === data.val()))"
                },
                "blackPlayer": {
                  ".validate": "newData.isString() && ((!data.exists() && newData.val() === '') || (data.exists() && newData.val() === data.val()) || (data.exists() && data.val() === '' && newData.val() === auth.uid && newData.parent().child('lastAction').val() === 'join' && newData.parent().child('whitePlayer').val() !== auth.uid))"
                },
                "blackPlayerName": {
                  ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[A-Za-z0-9 _-]{1,11}$/)) && ((!data.exists() && newData.val() === '') || (data.exists() && newData.val() === data.val()) || (data.exists() && data.val() === '' && newData.parent().child('blackPlayer').val() === auth.uid && newData.parent().child('lastAction').val() === 'join'))"
                },
                "blackPlayerAvatar": {
                  ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^[a-z_]+$/)) && ((!data.exists() && newData.val() === '') || (data.exists() && newData.val() === data.val()) || (data.exists() && data.val() === '' && newData.parent().child('blackPlayer').val() === auth.uid && newData.parent().child('lastAction').val() === 'join'))"
                },
                "blackPlayerTheme": {
                  ".validate": "newData.isString() && (newData.val() === '' || newData.val().matches(/^(kitty|batman)$/)) && ((!data.exists() && newData.val() === '') || (data.exists() && newData.val() === data.val()) || (data.exists() && data.val() === '' && newData.parent().child('blackPlayer').val() === auth.uid && newData.parent().child('lastAction').val() === 'join'))"
                },
                "whiteTime": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 600 && ((!data.exists() && newData.val() === 600) || (data.exists() && (newData.val() === data.val() || ((newData.parent().child('lastAction').val() === 'move' && ((data.parent().child('turn').val() === 'w' && data.parent().child('whitePlayer').val() === auth.uid) || (data.parent().child('turn').val() === 'b' && data.parent().child('blackPlayer').val() === auth.uid))) || (newData.parent().child('lastAction').val() === 'reset' && (data.parent().child('whitePlayer').val() === auth.uid || data.parent().child('blackPlayer').val() === auth.uid) && newData.val() === 600)))))"
                },
                "blackTime": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && newData.val() <= 600 && ((!data.exists() && newData.val() === 600) || (data.exists() && (newData.val() === data.val() || ((newData.parent().child('lastAction').val() === 'move' && ((data.parent().child('turn').val() === 'w' && data.parent().child('whitePlayer').val() === auth.uid) || (data.parent().child('turn').val() === 'b' && data.parent().child('blackPlayer').val() === auth.uid))) || (newData.parent().child('lastAction').val() === 'reset' && (data.parent().child('whitePlayer').val() === auth.uid || data.parent().child('blackPlayer').val() === auth.uid) && newData.val() === 600)))))"
                },
                "createdAt": {
                  ".validate": "newData.isNumber() && ((!data.exists() && newData.val() > 0) || (data.exists() && newData.val() === data.val()))"
                },
                "lastUpdated": {
                  ".validate": "newData.isNumber() && newData.val() > 0"
                },
                "expiresAt": {
                  ".validate": "newData.isNumber() && newData.val() > now && newData.val() <= now + 7200000"
                },
                "lastActorUid": {
                  ".validate": "newData.isString() && newData.val() === auth.uid"
                },
                "lastAction": {
                  ".validate": "newData.isString() && newData.val().matches(/^(create|join|move|reset|presence)$/)"
                },
                "whitePresenceAt": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && ((!data.exists() && newData.val() > 0 && newData.parent().child('whitePlayer').val() === auth.uid) || (data.exists() && (newData.val() === data.val() || (newData.parent().child('whitePlayer').val() === auth.uid && newData.parent().child('lastAction').val().matches(/^(move|reset|presence)$/) && newData.val() > 0))))"
                },
                "blackPresenceAt": {
                  ".validate": "newData.isNumber() && newData.val() >= 0 && ((!data.exists() && newData.val() === 0) || (data.exists() && (newData.val() === data.val() || (data.val() === 0 && newData.parent().child('lastAction').val() === 'join' && newData.parent().child('blackPlayer').val() === auth.uid && newData.val() > 0) || (newData.parent().child('blackPlayer').val() === auth.uid && newData.parent().child('lastAction').val().matches(/^(move|reset|presence)$/) && newData.val() > 0))))"
                },
                "history": {
                  ".validate": true
                },
                "captured": {
                  ".validate": true
                },
                "$other": {
                  ".validate": false
                }
              }
            }
          }
        }
      }
    }
  }
}